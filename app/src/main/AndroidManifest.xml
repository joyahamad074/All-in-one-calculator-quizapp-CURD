<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash"
            android:theme="@style/spalshtheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".quizresult" />
        <activity android:name=".quizquestion3" />
        <activity android:name=".quizquestion2" />
        <activity android:name=".quizquestion1" />
        <activity android:name=".saveinfo" />
        <activity android:name=".quizmain" />
        <activity android:name=".calculate" />
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>